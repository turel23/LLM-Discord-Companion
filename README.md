LLM powered discord bot that acts realistically
The bot will automatically create a memory folder, and uses memory based on facts and events.
Planning to add forgetting curve and adaptive memory that changes due to the message and other context.
So far the features are being able to read the past ~15 messages when starting up (because Discord shows past messages) and is able to recall past conversation and facts about the user.
